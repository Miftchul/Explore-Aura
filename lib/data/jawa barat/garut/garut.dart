import 'package:flutter/material.dart';

class Garut extends StatelessWidget {
  const Garut({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kota Garut'),
      ),
      body: Center(
        child: Text('Halaman Kota Garut'),
      ),
    );
  }
}