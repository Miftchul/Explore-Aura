import 'package:flutter/material.dart';

class EsalpukatPage extends StatelessWidget {
  const EsalpukatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('EsalpukatPage')),
      body: const Center(child: Text('Ini adalah halaman EsalpukatPage')),
    );
  }
}
