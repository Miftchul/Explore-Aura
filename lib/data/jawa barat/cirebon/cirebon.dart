import 'package:flutter/material.dart';

class Cirebon extends StatelessWidget {
  const Cirebon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kota Cirebon'),
      ),
      body: Center(
        child: Text('Halaman Kota Cirebon'),
      ),
    );
  }
}