import 'package:flutter/material.dart';

class Denpasar extends StatelessWidget {
  const Denpasar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kota Denpasar'),
      ),
      body: Center(
        child: Text('Halaman Kota Denpasar'),
      ),
    );
  }
}