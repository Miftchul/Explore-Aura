import 'package:flutter/material.dart';

class Kuta extends StatelessWidget {
  const Kuta({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kota Kuta'),
      ),
      body: Center(
        child: Text('Halaman Kota Kuta'),
      ),
    );
  }
}