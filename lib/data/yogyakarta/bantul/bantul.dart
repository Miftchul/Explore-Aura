import 'package:flutter/material.dart';

class Bantul extends StatelessWidget {
  const Bantul({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bantul'),
      ),
      body: Center(
        child: Text('Halaman Bantul'),
      ),
    );
  }
}
