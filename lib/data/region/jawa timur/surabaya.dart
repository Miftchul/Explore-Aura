import 'package:flutter/material.dart';

class Surabaya extends StatelessWidget {
  const Surabaya({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kota Surabaya'),
      ),
      body: Center(
        child: Text('Halaman Kota Surabaya'),
      ),
    );
  }
}