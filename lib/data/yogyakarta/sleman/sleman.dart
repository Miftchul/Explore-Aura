import 'package:flutter/material.dart';

class Sleman extends StatelessWidget {
  const Sleman({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kota Sleman'),
      ),
      body: Center(
        child: Text('Halaman Kota Sleman'),
      ),
    );
  }
}